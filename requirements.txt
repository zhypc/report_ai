# AI助手后台服务依赖

flask>=2.0.0
flask-cors>=3.0.0
requests>=2.25.0
gunicorn>=20.0.0  # 生产环境部署

# 数据库支持（可选，根据需要安装）
sqlalchemy>=1.4.0  # MySQL 支持
pymysql>=1.0.0     # MySQL 驱动
# mysqlclient>=2.0.0  # MySQL 驱动（可选，性能更好）
